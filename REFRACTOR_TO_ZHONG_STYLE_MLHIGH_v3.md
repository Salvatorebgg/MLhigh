# MLhigh → jichutongji_zhong 工作流重构 v3

## 本轮修复

1. 示例数据逻辑统一
   - 删除多套分散示例 CSV；
   - 仅保留一个 `comprehensive_example.csv`；
   - 所有方法的 `example_dataset` 均指向该综合示例；
   - 综合示例包含纵向数据、处理/分组、结局、协变量、生存、LDSC、机器学习标签等字段，可覆盖全部高级统计和机器学习方法。

2. 数据上传界面
   - 点击“加载示例”后直接载入综合示例；
   - 页面显示数据表格预览；
   - 示例库只保留“综合临床高级统计示例数据”。

3. jichutongji_zhong 式流程逻辑
   - 数据上传 → 选择变量 → 自动判断方法可用性 → 选择方法 → 参数配置 → 执行分析；
   - 方法是否可用由变量角色和变量类型共同判断；
   - 不适用方法自动变灰并显示原因。

4. 运行分析错误修复
   - PSM 处理变量改为 0/1 数值型；
   - CNN 标签改为二分类；
   - LDSC 增加 trait/h2/h2_se/rg 矩阵字段；
   - 生存分析在缺少 lifelines 环境时自动使用内置简化 KM/风险比估计，避免运行时报错。

5. 前端流程按钮修复
   - 恢复并强化左侧步骤与“下一步”按钮的工作流跳转；
   - 运行后仍能跳转到结果可视化。

## 检查

- 前端 JS 语法检查通过；
- `app/main.py`、`sample_service.py`、`stats_service.py` 编译通过；
- `tests/smoke.py` 通过。
