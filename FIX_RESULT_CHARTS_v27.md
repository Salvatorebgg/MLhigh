# v27 结果页修复说明

本版重点修复 v26 后结果可视化为空、诊断评估图显示不完整、图形过大、诊断图与结果可视化重复等问题。

## 关键修复
1. 修复结果可视化空白
   - v26 错误写入不存在的 `#chartPreviewMain`。
   - v27 改回真实容器 `#chartPreviewContainer`。

2. 缩小并居中图片
   - 主图限制为较小的居中画布。
   - 诊断图也使用独立较小画布。

3. 诊断评估与结果可视化去重
   - 诊断评估只显示独立诊断图。
   - 不再重复显示结果可视化已有的模型图。

4. 诊断图稳定渲染
   - 后端诊断图改为更稳定的完整性诊断与离群点诊断。
   - 前端诊断图容器重新收紧，避免显示不全。

5. 坐标轴箭头重做
   - 统一使用 Plotly annotation 箭头。
   - 去掉多套旧 shape/path 轴线互相覆盖的问题。

## 检查
- charts.js 语法检查通过。
- app.js 语法检查通过。
- main.py 编译通过。
- Lasso 示例可返回 5 张结果图 + 2 张独立诊断图。
